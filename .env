# OPTIONAL: proxy used ONLY during image build (pip/git inside Dockerfile).
# Leave empty (or remove this line) when your machine has normal internet access.
# If set, it must be reachable from Docker build containers: with Docker Desktop
# on WSL2 use the WSL host IP (`hostname -I`), not 127.0.0.1.
WSL_PROXY=http://172.30.93.196:11101
